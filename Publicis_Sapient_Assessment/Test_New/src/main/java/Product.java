/*
class Product
{
    enum colors
    enum size
    name;
    id;
}

interface ColoredProduct {
    List<Product> filterByColor(List<Product> productList, Colors colors);
}

class FilterColorProduct implements ColoredProduct {

    // @Autowired
    // private ProductRepository productRepository;
    // https://www.intervue.io/ZXZXY1FAW

    @Override
    public List<Product> filterByColor(List<Product> productList, Colors colors) {
        productList
            .stream()
            .filter(p -> p.getColor().)
    }





}
*/
